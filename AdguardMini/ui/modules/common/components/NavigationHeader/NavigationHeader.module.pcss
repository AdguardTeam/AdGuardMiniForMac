/* 
    SPDX-FileCopyrightText: AdGuard Software Limited
    SPDX-License-Identifier: GPL-3.0-or-later
*/

.NavigationHeader {
    display: flex;
    cursor: pointer;
    margin-top: var(--m-s);
    padding: var(--m-xxs) var(--m-xxs);
    margin-right: auto;
    width: max-content;
    word-break: keep-all;
    height: 40px;
    align-items: center;

    /*
     * Keyboard focus indicator: a standalone button-like header (its own
     * rounded hover surface), so the ring uses the button default (offset 0)
     * and no local override.
     */
    @mixin focus-ring;

    &:hover {
        border-radius: 8px;
        background: var(--fills-buttons-secondary-button-hovered);
    }

    & > * {
        display: inline-block;
    }

    &_icon {
        /* margin-top: 6px; */
        width: 24px;
    }

    &_text {
        margin-left: var(--m-xxs);
    }
}
