/* 
    SPDX-FileCopyrightText: AdGuard Software Limited
    SPDX-License-Identifier: GPL-3.0-or-later
*/

.FilterGroupPage {
    display: flex;
    flex-flow: column;

    &_addFilter {
        margin-top: var(--m-s);
        display: flex;
        color: var(--fills-buttons-main-button-default);
        cursor: pointer;
        

        &:hover {
            color: var(--fills-buttons-main-button-hovered);
        }
        padding: var(--m-xxs) var(--m-s);

        /*
         * Keyboard focus indicator: a padded list-row action (same row shape
         * as the `SettingsItem` rows below), so the contained preset keeps
         * the ring inside the row.
         */
        --focus-ring-offset: var(--focus-ring-offset-contained);

        @mixin focus-ring;

        &_text {
            margin-left: var(--m-xs);
        }
    }

    &_emptyResult {
        height: auto;
        width: auto;
        margin: auto 0;

        &_icon {
            width: 64px;
            height: 64px;
            margin-left: auto;
            margin-right: auto;
            margin-top: auto;
        }

        &_text {
            color: var(--text-description-description-default);
            text-align: center;
            margin-bottom: auto;
        }
    }

}