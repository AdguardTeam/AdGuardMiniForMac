/** Check whether referrer is valid. */
export declare function isValidReferrer(referrer: string): boolean;
/** Check whether string can be a user-agent. */
export declare function isValidUserAgent(string: string): boolean;
