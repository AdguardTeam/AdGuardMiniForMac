/**
 * Hook is used for handling press on Esc
 * @param func callback function
 * @param deps array of dependencies
 * @param useCapture use capture phase
 */
export declare function useEscape(func: () => void, deps?: unknown[], useCapture?: boolean | EventListenerOptions): void;
