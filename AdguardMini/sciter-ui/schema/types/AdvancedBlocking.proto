syntax = "proto3";

option optimize_for = CODE_SIZE;

// Describes AdvancedBlocking settings page
message AdvancedBlocking {
    bool advanced_rules = 1;
    bool adguard_extra = 2;
    bool mail_protection_enabled = 3;
    bool real_time_filters_update = 4;
}
